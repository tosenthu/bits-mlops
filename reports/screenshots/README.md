# Screenshots

Add screenshots here before final submission:

- MLflow experiment page with metrics and artifacts.
- GitHub Actions workflow run.
- Docker build and local API `/predict` response.
- Kubernetes `kubectl get pods,svc` and API verification.
- Prometheus/Grafana or `/metrics` endpoint evidence.

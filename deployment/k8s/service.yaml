apiVersion: v1
kind: Service
metadata:
  name: heart-disease-api
  namespace: bits-mlops
  labels:
    app: heart-disease-api
spec:
  type: NodePort
  selector:
    app: heart-disease-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
